[package]
name = "defaults-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
defaults-components = { path = "../components" }
plait = { path = "../../../..", default-features = false }
